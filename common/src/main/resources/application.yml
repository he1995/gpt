spring:
  application:
    name: common

  data:
    # redis??
    redis:
      # Redis?????????0?
      database: 1
      # Redis?????
      host: 127.0.0.1
      # Redis???????
      port: 6379
      # Redis?????????????
      # password:
      # ??????
      timeout: 10s
      lettuce:
        pool:
          # ????????
          max-active: 200
          # ???????????????????????
          max-wait: -1ms
          # ???????????
          max-idle: 10
          # ???????????
          min-idle: 0
